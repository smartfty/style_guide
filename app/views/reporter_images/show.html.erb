

<div align="center">
<div class='row'>
  <div class='col-md-2'>
  </div>

  <div class='col-md-3'>
    <p>
      <img style="object-fit:cover",  src="<%= @reporter_image.thumb_path %>" width="200px" height="200px"></img>
    </p>

    <p>
      <strong>Title:</strong>
      <%= @reporter_image.title %>
    </p>

    <p>
      <strong>Caption:</strong>
      <%= @reporter_image.caption %>
    </p>

    <p>
      <strong>Source:</strong>
      <%= @reporter_image.source %>
    </p>

    <p>
      <strong>Section name:</strong>
      <%= @reporter_image.section_name %>
    </p>

  </div>
  <div class='col-md-3'>
    <%= render 'form', reporter_image: @reporter_image%>
  </div>
</div>


<%= link_to '이전으로', my_reporter_images_path %>
<%= link_to '삭제', @reporter_image, method: :delete, data: { confirm: '정말 삭제하겠습니까?' } %>

</div>

