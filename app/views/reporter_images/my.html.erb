<!-- p id="notice"><%= notice %></p-->

<div align="center", style="padding: 0px 100px;">
<h3>나의 사진</h3>

<table class="table table-striped">
  <thead>
    <tr>
      <th>날짜</th>
      <th>제목</th>
      <th>사진설명</th>
      <th>출처</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reporter_images.each do |story| %>
      <% if story.selected%>
        <tr style="background-color:LightGreen;">
      <% else story.selected%>
      <tr>
      <% end %>
        <td><%= story.date_created %></td>
        <td><%= story.title %></td>
        <td><%= story.caption %></td>
        <td><%= story.source %></td>
        <td><%= button_to '수정', story , class: "btn btn-sm btn-warning", method: :get%></td>
        <td><%= button_to '삭제', story, class: "btn btn-sm btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '새로운 기자 사진 생성', new_reporter_image_path, method: :get, class: "btn btn-md btn-success" %>

</div>