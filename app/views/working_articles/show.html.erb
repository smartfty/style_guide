<p id="notice"><%= notice %></p>

<%=  @working_article.kind %>
<div class="row">
  <div class = "col-sm-6" style="padding-bottom: 40px;" width="100%">
    <% if @working_article.kind == '사설' || @working_article.kind == 'editorial' %>
      <%= render 'editorial', article: @working_article, page_number:  @working_article.page_number%>
    <% elsif @working_article.kind == '기고' || @working_article.kind == 'opinion' %>
      <%= render 'opinion', article: @working_article %>
    <% else %>
      <%= render 'form', article: @working_article %>
    <% end %>
  </div>
  <div class = "col-sm-1" style="padding-bottom: 40px;" width="100%">
    <p> </p>
  </div>

  <div class= "col-sm-5">
    <div class="button-group">
        <%= link_to "이전으로", page_path(@working_article.page), method: :get, class: "btn btn-md btn-success" %>
    </div>

    <br>
    <br>
    <% if browser.safari? %>
      <img width="100%" height="100%" src="<%= @working_article.pdf_image_path %>" ></img>
    <% elsif browser.modern?  || browser.ie?%>
      <img width="100%" height="100%" src="<%= @working_article.jpg_image_path %>" ></img>
    <% end %>

    <% if @working_article.article_info && @working_article.article_info[:overflow_text] && @working_article.article_info[:overflow_text] !=""%>
      <div align="center" width="50%" height="50px" style="color:red;"><%= @working_article.article_info[:overflow_text]%></div>
    <% elsif @working_article.article_info && @working_article.article_info[:empty_lines] %>
      <p align="center" >빈줄 수: <%= @working_article.article_info[:empty_lines] %></p>
    <% end %>



    <div align="center">
      <%= button_to "다운로드 샘플 PDF", download_pdf_working_article_path(@working_article), method: :get, class: "btn btn-sm btn-danger" %>
    </div>
    <br>
    <br>
    <div class="row">
      <% @working_article.images.each do |image| %>
        <div class="col-md-3">
          <p><a href="<%=  edit_image_path(image) %>"><%= image_tag image.image.url %></a></p>
          <p><%= link_to '삭제', image, method: :delete, data: { confirm: 'Are you sure?' } %></p>
        </div>
      <% end %>
    </div>

  </div>
</div>

<% # link_to "이미지 추가", add_image_article_path(@working_article), method: :get, class: "btn btn-sm btn-danger" %>
<% # link_to "저자 이미지 추가", add_personal_image_article_path(@working_article), method: :get, class: "btn btn-sm btn-danger" %>

<%= link_to "발문 추가", add_quote_article_path(@working_article), method: :get, class: "btn btn-sm btn-danger" %>

<div class="button-group">
    <%= link_to "이전으로", page_path(@working_article.page), method: :get, class: "btn btn-sm btn-success" %>
</div>
