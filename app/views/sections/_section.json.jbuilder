json.extract! section, :id, :profile, :column, :row, :ad_type, :is_front_page, :story_count, :page_number, :section_name, :layout, :color_page, :created_at, :updated_at
json.url section_url(section, format: :json)
