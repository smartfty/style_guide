<p id="notice"><%= notice %></p>
<div align="center">
  <%= link_to "이전으로", issue_path(@page.issue), method: :get, class: "btn btn-lg btn-success" %>
</div>

<div class="row">
  <div class = "col-md-1">
    <p> </p>
  </div>
  <div class = "col-md-10">
    <% if File.exist?(@page.pdf_path) %>
      <%= button_to "다운로드 샘플 PDF", download_pdf_page_path, method: :get, class: "btn btn-sm btn-danger" %>
      <%= raw @page.to_svg %>
      <%= button_to "다운로드 샘플 PDF", download_pdf_page_path, method: :get, class: "btn btn-sm btn-danger" %>
    <% end %>
  </div>

  <div class = "col-md-1">
    <p> </p>
  </div>

</div>

<div align="center">
  <%= link_to "이전으로", issue_path(@page.issue), method: :get, class: "btn btn-lg btn-success" %>
</div>

<br>
<br>



<div class="row">
  <div class = "col-md-2">
    <p> </p>
  </div>
  <div class = "col-md-8">
    <div class="row">
      <% @page_templates.each do |template| %>
      <div class="col-lg-2" >
        <% if template.id == @page.template_id %>
          <div class='selected_template'>
            <img width="98%"  src="<%= template.pdf_image_path %>" alt="" >
            <div align="center">
              <h4><%= template.profile =%></h4>
            </div>
          </div>
        <% else %>
          <div class='unselected_template'>
            <p><a href="<%=  change_template_page_path(@page.id, template_id: template.id) %>"><img  src="<%= template.pdf_image_path %>" width="100%"></img></a></p>
            <div align="center">
              <h4><%= template.profile =%></h4>
            </div>
          </div>
        <% end %>
      </div>
      <% end %>

    </div>


  </div>
  <div class = "col-md-2">
    <p> </p>
  </div>
</div>

<div align="center">
  <%= link_to "저장된 단락 스타일로 페이지 재생성", regenerate_pdf_page_path(@page), method: :get, class: "btn-group btn btn-sm btn-primary" , align: "center"%>
</div>
