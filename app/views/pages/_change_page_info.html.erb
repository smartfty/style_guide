<%= form_for(@page, remote: true) do |f| %>
  <% if page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <!-- %= f.check_box :color_page, :onclick => "this.form.submit()" % -->
    <%= f.check_box :color_page %>
    <%= f.button "컬러면 저장하기", class: "btn btn-md btn-info", remote: true, data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> 처리중..."} %>
  </div>
<% end %>


