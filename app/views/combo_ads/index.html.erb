<nav class="navbar navbar-default navbar-fixed-top2">
  <div class="btn-group" role="group" aria-label="기능">
    <button type="button" class="btn btn-info"><%= link_to "새항목 생성", new_combo_ad_path, method: :get %></button>
    <button type="button" class="btn btn-warning"><%= link_to "다운로드 CSV", combo_ads_path(format: "csv"), method: :get %>
    </button>
  <div>
</nav>

<h1>합성광고</h1>

<table class="responsive nowrap table table-hover" id="dttb-combo_ads" data-sort="true" data-source="<%= url_for(format: :json)%>">
  <thead>
    <tr>
      <th data-data="base_ad">Base ad</th>
      <th data-data="column">Column</th>
      <th data-data="row">Row</th>
      <th data-data="layout">Layout</th>
      <th data-data="profile">Profile</th>
      <th data-data="url" data-orderable="false" data-class-name="all" data-searchable="false" class="skip-export" width="100px"></th>
    </tr>
  </thead>

</table>

<br>
