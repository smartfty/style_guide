<%= simple_form_for(opinion_writer) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: "이름"   %>
    <%= f.input :title, label: "제목" %>
    <%= f.input :work, label: "소속" %>
    <%= f.input :position, label: "직책" %>
    <%= f.input :email, label: "이메일" %>
    <%= f.input :category_code, label: "코드" %>
    <%= f.input :opinion_image, label: "EPS선택" %>
    <%= f.input :opinion_jpg_image, label: "JPG선택" %>
    <%= f.input :publication_id, :as => :hidden, input_html: {value: 1} %>
  </div>

  <!-- 편집화면 상단에 자주쓰는 이전, 저장, 다운 버튼을 항상 보이게 설정 -->
  <nav class="navbar navbar-default navbar-fixed-top2">
  <div class="btn-group" role="group" aria-label="기능">
    <button type="button" class="btn btn-success"><%= link_to "이전으로", opinion_writers_path, method: :get %></button>
    <%= f.button :submit, class: "btn btn-primary", value: "저장" %>
    <button type="button" class="btn btn-danger"><%= link_to '삭제', @opinion_writers, method: :delete, data: { confirm: '정말 삭제하겠습니까?' } %></button>
  </div>
</nav>
<% end %>
