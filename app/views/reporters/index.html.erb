<p id="notice"><%= notice %></p>

<h1>Reporters</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Division</th>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reporters.each do |reporter| %>
      <tr>
        <td><%= reporter.name %></td>
        <td><%= reporter.email %></td>
        <td><%= reporter.division %></td>
        <td><%= reporter.title %></td>
        <td><%= link_to 'Show', reporter %></td>
        <td><%= link_to 'Edit', edit_reporter_path(reporter) %></td>
        <td><%= link_to 'Destroy', reporter, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Reporter', new_reporter_path %>
