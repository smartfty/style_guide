<!-- p id="notice"><%= notice %></p-->

<h1>Publications</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Paper size</th>
      <th>Width</th>
      <th>Height</th>
      <th>Left margin</th>
      <th>Top margin</th>
      <th>Right margin</th>
      <th>Bottom margin</th>
      <th>Lines per grid</th>
      <th>Gutter</th>
      <th>Page count</th>
      <th>Section names</th>
      <th> page_columns</th>
      <th> 1면 제호 높이</th>
      <th> 내지 제호 높이</th>
      <th> 기사 하단 여백</th>
      <th> 기사 테두이선 위치</th>
      <th> 기사 하단 줄 굵기</th>
      <th> 세로 기사 분리선 그리기</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @publications.each do |publication| %>
      <tr>
        <td><%= publication.name %></td>
        <td><%= publication.paper_size %></td>
        <td><%= publication.width %></td>
        <td><%= publication.height %></td>
        <td><%= publication.left_margin %></td>
        <td><%= publication.top_margin %></td>
        <td><%= publication.right_margin %></td>
        <td><%= publication.bottom_margin %></td>
        <td><%= publication.lines_per_grid %></td>
        <td><%= publication.gutter %></td>
        <td><%= publication.page_count %></td>
        <td><%= publication.section_names %></td>
        <td><%= publication.page_columns %></td>
        <td><%= publication.front_page_heading_height %></td>
        <td><%= publication.inner_page_heading_height %></td>
        <td><%= publication.article_bottom_spaces_in_lines %></td>
        <td><%= publication.article_line_thickness %></td>
        <td><%= publication.draw_divider %></td>
        <td><%= link_to 'Show', publication %></td>
        <td><%= link_to 'Edit', edit_publication_path(publication) %></td>
        <td><%= link_to 'Destroy', publication, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Publication', new_publication_path %>
