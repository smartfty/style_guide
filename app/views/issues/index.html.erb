<!-- p id="notice"><%= notice %></p-->

<div align="center">

  <h3>이전 신문 보기</h3>

  최종 발행호<%= button_to "#{Issue.last.date}(#{Issue.last.number})", change_current_issue_path(Issue.last), method: :get, class: "btn btn-sm btn-primary"  %></td>

  <table class="table table-striped" style="width: 200px;">
    <thead
      <tr>
        <th>지난호</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @issues_for_search.each do |issue| %>
        <tr>
          <td>
          <%= button_to "#{issue.date}(#{issue.number})", change_current_issue_path(issue), method: :get, class: "btn btn-sm btn-primary"  %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= paginate @issues_for_search %>
</div>
