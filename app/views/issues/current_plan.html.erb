<p id="notice"><%= notice %></p>


<h3 align="center"><%= @issue.pages.first.korean_date_string %> : 면배열표</h3>
<div class='row'>
  <div class="col-md-2">
  <p> </p>
  </div >

  <div class="col-md-4 .col-md-offset-2">
    <table class="table table-striped">
      <thead>
        <tr>
          <th style="text-align:center;">페이지</th>
          <th style="text-align:center;">칼라</th>
          <th style="text-align:center;">면 이름</th>
          <th style="text-align:center;">광고</th>
          <th style="text-align:center;">광고주</th>
          <th style="text-align:center;">마감시간</th>
          <th style="text-align:center;">저장</th>
        </tr>
      </thead>
      <tbody>
        <% @front_page_plans.each do |page_plan| %>
          <%= form_for(page_plan) do |f| %>
          <% if page_plan.color_page %>
            <tr style="background-color:pink;">
          <% else %>
            <tr style="background-color:white;">
          <% end %>
              <td style="text-align:center;"><%= page_plan.page_number %></td>
              <td style="text-align:center;"><%= f.check_box :color_page %></td>
              <td style="text-align:center;"><%= f.select :section_name, %w[1면 정치 자치행정 국제통일 금융 산업 정책 기획 오피니언 전면광고], {include_blank: false} %></td>
              <td style="text-align:center;"><%= f.select :ad_type, %w[광고없음 3단통 4단통 5단통 6단통 7단통 7단15 7단25 8단통 8단27 8단_브릿지 9단21 15단통 15단_브릿지], {include_blank: false} %></td>
              <td style="text-align:center;"><%= f.select :advertiser, %w[사계절 부영 이마트 삼성 농협은행 일동제약 삼성 CJ KT LG하우시스 SK이노베이션 SK하이닉스 SK텔레콤 YTN 연합뉴스 내일교육 석간내일신문 내일이비즈 굿네이버스 녹십자 기아차 현대차 국민지주 하나은행 다상대상 근로복지공단 산업안전보건공단 산업인력공단 한국관광공사 한수원 한마당서림 공고 청산공고 분실공고 서부발전 도서관협회 유한양행 애경], {include_blank: true} %></td>
              <td style="text-align:center;"><%= f.text_field :deadline ,class: 'page-plan-form' %></td>
              <% if page_plan.page%>
              <td align="right"><button type="submit" class="btn btn-sm btn-primary">저장</button></td>
              <% else %>
                <td align="right"><button type="submit" class="btn btn-sm btn-primary">저장</button></td>
              <% end %>
            </tr><% end %>
        <% end %>
      </tbody>
    </table>
 </div>
  <div class='col-md-4'>
    <table class="table table-striped">
      <thead>
        <tr>
        <th style="text-align:center;">페이지</th>
        <th style="text-align:center;">칼라</th>
        <th style="text-align:center;">면 이름</th>
        <th style="text-align:center;">광고</th>
        <th style="text-align:center;">광고주</th>
        <th style="text-align:center;">마감시간</th>
        <th style="text-align:center;">저장</th>
        </tr>
      </thead>
    <tbody>
      <% @back_page_plans.each do |page_plan| %>
        <%= form_for(page_plan) do |f| %>
        <% if page_plan.color_page %>
          <tr style="background-color:pink;">
        <% else %>
          <tr style="background-color:white;">
        <% end %>
        <td style="text-align:center;"><%= page_plan.page_number %></td>
        <td style="text-align:center;"><%= f.check_box :color_page %></td>
        <td style="text-align:center;"><%= f.select :section_name, %w[1면 정치 자치행정 국제통일 금융 산업 정책 기획 오피니언 전면광고], {include_blank: false} %></td>
        <td style="text-align:center;"><%= f.select :ad_type, %w[광고없음 3단통 4단통 5단통 6단통 7단통 7단15 7단25 8단통 8단27 8단_브릿지 9단21 15단통 15단_브릿지], {include_blank: false} %></td>
        <td style="text-align:center;"><%= f.select :advertiser, %w[사계절 부영 이마트 삼성 농협은행 일동제약 삼성 CJ KT LG하우시스 SK이노베이션 SK하이닉스 SK텔레콤 YTN 연합뉴스 내일교육 석간내일신문 내일이비즈 굿네이버스 녹십자 기아차 현대차 국민지주 하나은행 다상대상 근로복지공단 산업안전보건공단 산업인력공단 한국관광공사 한수원 한마당서림 공고 청산공고 분실공고 서부발전 도서관협회 유한양행 애경], {include_blank: true} %></td>
        <td style="text-align:center;"><%= f.text_field :deadline ,class: 'page-plan-form' %></td>
        <% if page_plan.page%>
                <td align="right"><button type="submit" class="btn btn-sm btn-primary">저장</button></td>
              <% else %>
                <td align="right"><button type="submit" class="btn btn-sm btn-primary">저장</button></td>
              <% end %>
          </tr><% end %>
      <% end %>
    </tbody>
  </table>
  </div>
</div>
<br>

<% if @front_page_plans.first.page %>
<% else %>
  <div align="center">
    <%= link_to "페이지 생성", update_plan_issue_path(Issue.last.id), method: :get, class: "btn-group btn btn-md btn-info" %>
  </div>
<% end %>
