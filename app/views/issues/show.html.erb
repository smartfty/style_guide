
<button class="btn-danger large"><%= @issue.date.to_s %> <%= @issue.number %></button>
<div class="row">
  <% @issue.pages.each do |page| %>
  <div class = "col-sm-3 col-md-3"  align='center'>
  <a href="<%=  page_path(page) %>"><img  src="<%= page.jpg_image_path %>" width="100%"></img></a>
    <h4 align='center'><a href='<%= page_path(page)%>'><%= page.page_number %></a></h4>
  </div>    <!-- true -->
  <% end %>

</div>

<%= button_to "xml 파일 생성", save_story_xml_issue_path, method: :get, class: "btn btn-sm btn-warning" %>
<%= button_to "xml 파일 다운로드", download_story_xml_issue_path, method: :get, class: "btn btn-sm btn-danger" %>
