

<div class="view">
    <% @pages.each do |page| %>
      <div class = "col-xs-6 col-md-3"  align='center'>
      <!--a href="<%=  page_path(page) %>"><img  src="<%= page.jpg_image_path %>" width="100%"></img></a-->
      <%= raw page.to_svg_with_jpg %>
      <h4 align='center'><%= link_to "#{page.page_number}면", page_path(page), class: "btn btn-default btn-sm"  %></h4>    </div>    <!-- true -->
    <% end %>
</div>
