<%= form_for(graphic) do |f| %>
  <% if graphic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(graphic.errors.count, "error") %> prohibited this graphic from being saved:</h2>

      <ul>
      <% graphic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
  <div class="col-xs-12 col-md-7">
      <%= image_tag graphic.graphic.url, :width => "100%", style: "padding-bottom:20px" %>
 </div>
 <div class="col-xs-12 col-md-5">

        <div class="field">
        <%= f.label :위치 %><%= f.text_field :position, style: "width:35px", class: 'form-control' %>
        <%= f.label :크기 %><%= f.text_field :column, style: "width:35px", class: 'form-control' %>단  x  <%= f.text_field :row, style: "width:35px", class: 'form-control' %>열
        <%= f.text_field :extra_height_in_lines, style: "width:35px", class: 'form-control' %>행 추가
        </div>

        <!-- div class="field">
          <%= f.label '페이지' %>
          <%= f.text_field :page_number %>
        </div>

        <div class="field">
          <%= f.label '기사 번호' %>
          <%= f.text_field :story_number %>
        </div -->

        <div class="actions">
          <%= f.button "수정", class: "btn btn-primary btn-sm" %>
          <%= link_to '삭제', graphic, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-md btn-danger" %>

        </div>
      </div>  
  </div>

<% end %>
